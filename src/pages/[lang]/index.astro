---
// src/pages/[lang]/index.astro
import BaseLayout  from '../../layouts/BaseLayout.astro';
import QrGenerator from '../../components/QrGenerator';
import AdTop       from '../../components/ads/AdTop.astro';
import AdSidebar   from '../../components/ads/AdSidebar.astro';
import AdBottom    from '../../components/ads/AdBottom.astro';
import { LOCALES, getTranslations } from '../../i18n';
import type { Locale } from '../../i18n';

export function getStaticPaths() {
  return LOCALES.map((lang) => ({ params: { lang } }));
}

const lang           = Astro.params.lang as Locale;
const t              = getTranslations(lang);
const alternateLangs = LOCALES.filter((l) => l !== lang);
---

<BaseLayout
  title={t.meta.title}
  description={t.meta.description}
  lang={lang}
  canonicalUrl={`https://gqr.teamcellmania.com/${lang}/`}
  alternateLangs={alternateLangs}
  ogImageAlt={t.meta.og_image_alt}
>
  <div class="flex flex-col min-h-screen bg-[#0a0a0f]">

    <!-- Banner superior -->
    <div class="border-b border-white/[0.05] py-3">
      <AdTop />
    </div>

    <!-- App principal -->
    <div class="flex-1">
      <QrGenerator client:only="react" t={t} lang={lang} />
    </div>

    <!-- Banner sidebar 300×250 — solo desktop, alineado bajo el aside -->
    <div class="max-w-5xl mx-auto w-full px-4 pb-4 flex justify-end">
      <AdSidebar />
    </div>

    <!-- Banner inferior -->
    <div class="border-t border-white/[0.05] py-6">
      <AdBottom />
    </div>

  </div>
</BaseLayout>